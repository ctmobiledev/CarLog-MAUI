<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<!--
	Doing custom icons:
	https://learn.microsoft.com/en-us/dotnet/maui/user-interface/images/app-icons?tabs=android
	https://stackoverflow.com/questions/72863363/how-to-fix-resource-error-from-android-manifest
	
	MAUI does some tricky stuff, hidden, under the covers. The "drawable" and "mipmap" folders that normally are visible
	in a native Android project are generated by MAUI, and are HIDDEN.
	
	The icon file name after "@mipmap" is the root name of the icon file, but WITHOUT the extension (svg or png).
	
	The original file had a file "appicon.svg" so the default project used this:
		android:icon="@mipmap/appicon" android:roundIcon="@mipmap/appicon_round" 
		
	Since this project has a "carlogicon1.png" file, the root name "carlogicon1" was subbed in after "@mipmap". The resulting
    line, below, compiles.
	-->
	<application android:allowBackup="true" android:icon="@mipmap/carlogicon1" android:roundIcon="@mipmap/carlogicon1_round" android:supportsRtl="true"></application>
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />
</manifest>